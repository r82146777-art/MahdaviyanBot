name: DISABLED - moved to private repo

# بسته صبحگاهی به مخزن خصوصی منتقل شد.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use MahdaviyanBot-Private."
