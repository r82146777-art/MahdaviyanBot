name: DISABLED - moved to private repo

# این workflow غیرفعال شده است.
# ربات به مخزن خصوصی منتقل شد: MahdaviyanBot-Private
# دیگر از این مخزن عمومی پستی ارسال نمی‌شود.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This public workflow is DISABLED."
          echo "Bot moved to private repository: MahdaviyanBot-Private"
          exit 0
